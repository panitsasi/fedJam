[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fedjam-flower"
version = "1.0.0"
description = ""
license = "Apache-2.0"
dependencies = [
    "flwr[simulation]>=1.17.0",
    "flwr-datasets[vision]>=0.5.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.flwr.app]
publisher = "fj"

[tool.flwr.app.components]
serverapp = "fedjam_flower.server_app:app"
clientapp = "fedjam_flower.client_app:app"

[tool.flwr.app.config]
num-server-rounds = 2
fraction-fit = 1
local-epochs = 1
model_name = "google/vit-base-patch16-224-in21k"
learning-rate-max = 5e-4
learning-rate-min = 1e-6
batch_size = 128
dataset_version = "test"
data_dir = "/home/ioannis/Desktop/spectrograms/dataset_v3/spectro_flower_gain"
fft = 0
gain = 20
num_clients = 2
is_lora = false
is_timm = false
is_warmup = false
random_seed = 42

[tool.flwr.federations]
default = "local-simulation"

[tool.flwr.federations.local-simulation]
options.num-supernodes = 2
options.backend.client-resources.num-cpus = 24
options.backend.client-resources.num-gpus = 0.3
